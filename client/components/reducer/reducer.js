const initialState = {
  teaTypes: [],
};

const reducer = (state = initialState, action) => {
  // eslint-disable-next-line no-console
  console.log({ action });

  return state;
};

export default reducer;
